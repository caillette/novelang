--- Minor fix on ``JavaShell`` for cleaner shutdown when there is no default ``JmxKit``.
This only may affect users of `Novelang-attirail` subproject.

--- Fixed documentation generation where release notes for SNAPSHOT versions appeared for
non-SNAPSHOT versions.

